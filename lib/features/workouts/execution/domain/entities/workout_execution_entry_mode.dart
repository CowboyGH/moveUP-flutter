/// Supported entry points into the workout execution flow.
enum WorkoutExecutionEntryMode {
  /// Start or resume execution from warmups.
  warmup,

  /// Start or resume execution from the main workout.
  workout,
}
