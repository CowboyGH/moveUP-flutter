/// Tabs available in the profile statistics history dialog.
enum ProfileHistoryTab {
  /// Active subscription snapshot.
  subscriptions,

  /// Latest workout snapshot.
  workouts,

  /// Latest test snapshot.
  tests,
}
